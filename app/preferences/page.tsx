import { Form } from "../../components/Input";

export default async function Preferences() {

    return (
        <div>
            <h1>Preferences</h1>
            <Form></Form>
        </div>
    );

};
